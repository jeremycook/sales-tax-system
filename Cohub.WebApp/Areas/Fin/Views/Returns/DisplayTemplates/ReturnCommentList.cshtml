@model IEnumerable<ReturnComment>

<div sk-if="@Model?.Any() == true" style="max-height: 50vh; overflow-x: hidden; overflow-y: auto">
    <partial name="DisplayTemplates/CommentList" model="Model.Select(o => o.Comment).OrderByDescending(o => o.Id)" />
</div>
